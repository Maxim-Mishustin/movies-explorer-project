// Статус создания нового ресурса
const CREATED_CODE = 201;

// Ошибка сервера
const SERVER_ERROR = 500;

// Ключ для режима разработки
const AUTH_KEY_DEV = 'dev-secret';

module.exports = {
  AUTH_KEY_DEV,
  SERVER_ERROR,
  CREATED_CODE,
};
